{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "bun ---watch cmd/api/index.ts",
    "build": "bun build cmd/api/index.ts --target bun --outdir ./dist",
    "start": "NODE_ENV=production bun dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:drop": "drizzle-kit drop",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.8",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.7",
    "nanoid": "^5.1.6",
    "postgres": "^3.4.7",
    "zod": "^4.1.13"
  }
}
